module.exports = {
  root: true,
  // extends: '@react-native-community',
  parser: '@babel/eslint-parser',
  extends: ['airbnb', 'plugin:react-native/all'],
  plugins: ['react', 'react-native'],
  rules: {
    // 여기에 사용자 지정 규칙을 추가하거나 재정의합니다.
  },
};
